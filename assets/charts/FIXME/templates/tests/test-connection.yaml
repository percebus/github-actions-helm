apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "FIXME.fullname" . }}-test-connection"
  labels:
    {{- include "FIXME.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "FIXME.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
