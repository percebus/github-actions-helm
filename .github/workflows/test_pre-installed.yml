name: Test pre-installed

on:
  workflow_dispatch: # Allows you to run this workflow manually from the Actions tab
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  runs-on__matrix:
    strategy:
      fail-fast: false
      matrix:
        runs-on:
          - ubuntu-latest
          - macos-latest  # "Command not found"
          - windows-latest

    runs-on: ${{ matrix.runs-on }}
    name: pre-installed @ ${{ matrix.runs-on }}
    steps:
      - name: helm version
        run: helm version
        continue-on-error: true
